# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
build/
dist/
.coverage
htmlcov/
.tox/

# Virtual environments
.venv/
venv/
env/
.env
.envrc

# Environment files (NEVER commit secrets, see .env.example)
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# uv
.uv/

# Project-specific
# ------------------------------------------------------------------
# We commit input PDFs and verified archive projects, but never
# cache (regeneratable) or raw extraction outputs (not yet verified).

# Caches: regeneratable, large, change frequently
data/cache/

# Outputs: regenerated by each pipeline run
#data/outputs/

# Inputs are committed by default, except for the corrupted variant
# which gets generated at Stage 7b
data/inputs/draka_corrupted/
data/inputs/draka/
data/inputs/nieuw-legmeer/
# Streamlit
.streamlit/secrets.toml

# Test artifacts
.pytest_cache/
